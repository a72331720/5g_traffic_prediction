# Notebooks

Put exploratory Jupyter notebooks here, for example:

- `01_data_exploration.ipynb` — first look at the raw dataset
- `02_feature_engineering.ipynb` — try different feature sets
- `03_model_comparison.ipynb` — compare LR / RF / (optional) LSTM

Keep reusable logic in `src/`; notebooks should just import and call it.
